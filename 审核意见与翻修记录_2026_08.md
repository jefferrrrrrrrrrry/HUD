# 审核意见与翻修记录（2026-08）

> **本文件的性质**：以严格导师身份对全部交付物（5 份 HTML 汇报稿、5 份配套讲稿、5 份可编辑 PPTX、
> 论文正文与实验设计依据）连续三轮审核的意见清单与逐条处置记录。
> **审核对象清单**见文末附录 A；**校验入口**见附录 B。
> **记录规则**：每条意见给出「问题—依据—处置—复核方式」四段；凡处置涉及生成脚本者，
> 一律改在**生成源**而非改在产物，以免下次重建时回退。
> **本文件不受汇报稿表达禁则约束**：为保留可追溯性，文中逐字引用了修正前的原表述与预期中的追问原话，
> 故 `verify_style.py` 的默认目标集不含本文件；若单独对本文件运行该校验器，命中项均为上述引用。

---

## 第一轮　结构与口径一致性

本轮的审核标准：**同一事实在任何一份交付物中不得有两种写法；投影上的每一页必须能被目录唯一指称；
每一处效应表述必须给出方向与量。** 本轮共提出 8 条，全部已处置。

### R1-01　专题综述 deck 出现两页同名标题

- **问题**：`盲区框架/文献综述_盲区情境.html` 的 p06 与 p07 标题字面完全相同（「六处边界推翻：非盲区结论为何不可迁移」）。
- **依据**：答辩现场以标题指称页码是常规做法；同名两页使「请回到六处边界推翻那一页」这一指令不可执行。
  配套讲稿亦因此出现两个同名 `## pNN` 节。
- **处置**：拆为「**六处边界推翻（一）：零点、空间与上界**」与「**六处边界推翻（二）：信任、视场与理论**」。
  改动落在 `scripts/build_blind_review_deck.py` 的 `bd_page()` 函数（新增 `title` 形参），
  讲稿标题由生成脚本自 deck 读取，故自动同步；讲稿 p07 首句「续前页三条」改为「续前页，本页是信任、视场与理论三处」。
- **复核**：`audit_all.py [B]` 标题唯一性通过；`verify_deck_talk.py` 14 页标题逐字一致。

### R1-02　专题综述 deck 未交代因变量归属

- **问题**：该 deck 通篇 0 次出现「情境意识」，四类因变量构念一次未齐。
- **依据**：本专题以注意加工阶段（扫描／识别／预测）组织论证，但**阶段不是构念**。
  导师必问「你的因变量是什么、归入哪一类构念、主要终点是哪一个」，而 deck 无处可指。
- **处置**：p14 小结页新增「因变量归属」块——四类构念、情境意识按三阶段计分、
  预告区段内预期性注视为预测层代理量、危险漏检率为并列主要终点、情境意识单列不并入认知负荷的可解离依据；
  讲稿 p14 与「只说一句」同步补入。
- **复核**：`shoot_slides.py` 14 页零溢出；四类构念齐备。

### R1-03　双情境两份 deck 封面信息不足

- **问题**：`研究汇报_双情境框架.html` 封面仅有主副标题；`开题报告_双情境框架.html` 仅多一行汇报性质。
  两份 deck 的题名与主线题名不同，而封面未声明二者关系。
- **依据**：同一答辩场合出现两个题名，若不显式声明「同一课题的两种切分方案」，
  最可能的追问是「你到底做哪一个」，且该追问会占用主体时间。
- **处置**：两份封面均新增「与主线题目的关系」声明（主线按参数类型切分／本框架按视线可见性切分、
  共用同一文献库与同一四类构念与同一判定顺序）、版本与配套讲稿信息；研究汇报稿另加核心论断块。
  同时补齐两份 deck 缺失的 `.cover .q` 与 `.cover .rq` 样式定义——原文件无此二类，
  若不补则新增文本以无样式的裸文本呈现。
- **复核**：两份 deck 重截零溢出。

### R1-04　双情境研究汇报 p12 版心溢出 13 px

- **问题**：`研究汇报_双情境框架.html` p12（研究设计总表页）实测 `scrollHeight 611 / clientHeight 598`。
  该页此前被记为零溢出，属**报告过期**——溢出报告未随最后一次内容修改重跑。
- **处置**：① 该页改用 `.dense` 紧排（行间距 11 → 8 px）并为紧排页单独收紧 `.tiny` 单元格内边距；
  ② 压缩两处冗语（「招募约束大幅放宽」「决策模糊带」等），实质信息不减。
- **复核**：`shoot_slides.py` 13 页零溢出。**并据此确立一条纪律**：凡改 deck 必重跑截图探针，
  否则 `verify_blind.py` 第 7 项校验的是旧报告。

### R1-05　三处效应表述未给方向与量

- **问题**：`AR-HUD行人碰撞预警_毕业论文研究框架.md`「解释预警为何有效」；
  `thesis/第2章_文献综述_v2.md`「2.5 s 无效、3.0 s 起有效」与「为什么动态空间信息有效」。
- **依据**：本课题已在 `verify_style.py` 中把「有效／无效」列为禁用表述，理由是它把
  「与何条件比较、在哪个指标上、效应多大」三项信息一并省略；自订规则须自守。
- **处置**：分别改为「解释预警**在哪一加工阶段产生增益**」、「2.5 s **与无预警条件无显著差异**，
  3.0 s 起**出现显著改善**」、「**动态空间信息何以产生增益**……其增益来源在于」。
- **复核**：`audit_all.py [A]` 无残留。

### R1-06　投影数值与口播覆盖不一致

- **问题**：deck 上出现而讲稿未提及的数值，主线汇报稿 5 处、专题综述稿 10 处、
  文献综述稿逾百处（表内统计量）。
- **依据**：投影上的任何数值都可能被指着问「这个数是哪来的」。但要求逐字稿覆盖表内每一个统计量并不现实，
  故须**明文规定覆盖口径**，而不是默认全覆盖。
- **处置**：① 五份讲稿统一加入「**数值口播约定**」——表内统计量、括注内次级参数与备查页数值属备查性质，
  讲稿只覆盖参数锚点数值、结论冲突两端数值与本研究推导的校验值，其余供追问时逐格指读、不进入计时；
  ② 主线讲稿 p12 补入 0.16 s 制动响应参考量级的来源与替换规则（该值为投影上唯一未被口播的**研究者设定值**）。
- **复核**：`audit_all.py [C]` 余项均属表内统计量，已在约定覆盖范围内。

### R1-07　审计工具误报

- **问题**：三处**合规的反面引用**被判为旧术语残留（「BTN 不可读作『基准时间节点』」等）。
- **依据**：审计工具的误报会稀释后续轮次的信号，属工具缺陷而非文本缺陷。
- **处置**：`scripts/audit_all.py` 豁免词表补入「不可读作／不得读作／而非「／无对应测量构念／不可合称」；
  数值扫描前先归一化数字中的窄空格千分位（`3 700` → `3700`），并排除小数点前后误切片。
- **复核**：`audit_all.py` 由 10 处待修降至 0，且无一处为掩盖式豁免。

### R1-08　`verify_style.py` 的适用范围过宽

- **问题**：专题综述稿本无研究目标页，却被要求出现 RQ1–RQ3、O1–O4 与四类构念，产生 4 条不可能满足的失败。
- **处置**：新增 `in_scope()`——仅对含「研究目标」的汇报稿施加第 [3]、[4] 项，其余稿件打印「不适用」。
- **复核**：主线四件与专题两件均通过，且不再有伪失败。

---

## 第二轮　计时可行性、引注可追溯与应答覆盖

本轮的审核标准：**讲稿声明的时长必须在物理上可讲完；投影上的每一处引注必须能在文献库中查到；
每一页讲述内容都必须预备至少一条追问的答案。** 本轮共提出 5 条，全部已处置。

### R2-01　全部讲稿的计时声明在物理上不可实现　（本轮最重）

- **问题**：以 245 字/分钟的中文学术口播速率核算，五份讲稿的逐字部分实际朗读时长与声明配额之比为
  **1.8× 至 3.3×**：主线汇报稿逐字 30 分／声明 16 分 10 秒；文献综述稿逐字 110 分／声明 33 分 10 秒；
  双情境研究汇报稿 35 分／19 分 40 秒；开题报告稿 49 分／20 分 50 秒；专题综述稿 32 分／17 分 0 秒。
  换算成语速，部分页面需 500 至 1900 字/分钟。
- **依据**：这不是估算误差而是**口径错误**——把「逐字稿的篇幅」当成了「讲述时长」。
  若按声明时长上台并试图念稿，必然在中途被打断；若按逐字稿讲，则超时一倍以上。
- **处置**：确立**两轨制计时口径**并写入五份讲稿头部：
  ① ⏱ 标注的是该页**讲述配额**，不是逐字稿朗读时长；
  ② 逐字稿全文朗读时长按 245 字/分钟实测给出（各稿分别为 30／110／35／49／32 分钟），
     并明示**不用于全文朗读**，其作用是备稿与应答；
  ③ 按配额讲述时以该页「只说一句」为骨架、按需展开逐字稿中的加粗要点；
  ④ 逐页核验「只说一句」可在本页配额内讲完（≤ 320 字/分钟），全部 102 页通过。
  校验器 `scripts/audit_round2.py [G]` 把该不变量固化为硬性检查，并把逐字倍率作为信息项报告。
- **复核**：五份讲稿全部通过 [G]；配额合计与文末计时一致。

### R2-02　专题综述讲稿应答覆盖仅 29%，且 5 个核心页无备答

- **问题**：`文献综述_盲区情境_讲稿.md` 14 页中仅 4 页有备答；★ 核心页 p06、p07、p10、p12、p14 全部无备答。
- **依据**：导师画像为「不喜字多、爱提问」，核心页恰是提问密度最高处；核心页无备答等于把最可能被问的地方空着。
- **处置**：在 `scripts/build_blind_review_talk.py` 中为 p04、p06、p07、p08、p09、p10、p11、p12、p13、p14
  共 10 页补入备答，每条针对一个具体的可预期追问，例如：
  「BD-1 是否只是测量困难而非物理上不存在」（给出可判据）、
  「17° 视场取自 2018 年硬件、是否已失效」（量产视场仍为 10–15°，约束更紧）、
  「若 P6′ 被否证前面论证是否作废」（前提论证不依赖迁移损失量级，理论区分成立而工程可合并）、
  「承认车载感知不足后结论对量产车是否仍有意义」（给出车路协同的人因需求指标一层与显露区段适用一层）。
- **复核**：`audit_round2.py [H]` 备答覆盖 100%（封面页豁免）。

### R2-03　主线汇报稿七个参数页无备答

- **问题**：`研究汇报_2026_08_讲稿.md` 的 p03–p09（参数一至参数七）全部无备答，覆盖率 71%。
- **依据**：参数页是本汇报的主体，且每页都以「本研究取值」作结——取值一旦上投影，必被追问依据。
- **处置**：逐页补入一条备答，针对该页取值最薄弱处，例如参数一「为何不直接用 3.0 s 下界」
  （绝对碰撞时间不可跨车速迁移，故只作校验不作取值）、参数四「滞环而非固定时长」的运动学理由、
  参数七「两个映射维度的拆分是否有文献支持」（本库无正交操纵，即空白 G7，拆分依据是编码通道不同）。
- **复核**：备答覆盖 100%。

### R2-04　引注可追溯性抽检

- **问题**：需确认 deck 与讲稿中的「作者 等（年份）」是否均可在文献综合表、参考文献列表、
  第 2 章或盲区证据台账中查到。
- **处置**：新增 `scripts/audit_round2.py [F]`——提取全部作者—年份式引注，在文献库四源加 `summaries/`
  文件名中作「作者名邻近 220 字符内出现该年份」的匹配。
- **复核**：五份 deck 引注全部可追溯，无孤立引注。

### R2-05　两页逐字稿越过单页上限

- **问题**：`文献综述_幻灯片_讲稿.md` p06（1588 字）与 p18（1303 字）越过单页 1300 字上限。
- **处置**：两页各加一行页内提示——「本页逐字稿偏长：配额内只讲『只说一句』加加粗要点，
  其余留作追问时展开」。**未删内容**，因该二页分别承担时机参数的证据梯度与冲突辨析，删减会破坏论证；
  以口径声明代替删减，与 R2-01 的两轨制一致。
- **复核**：`audit_round2.py [I]` 余两页为已声明的例外。

---

## 第三轮　可追溯性、口径同步与交付完整性

本轮的审核标准：**投影上的每一个数值都必须可追溯至来源——凡无文献来源者须自认；
任何一份交付物的口径不得落后于其上游；交付清单必须能让第三方在不询问作者的情况下复现全套校验。**
本轮共提出 8 条，全部已处置。

### R3-01　主线汇报稿封面缺版本与配套信息

- **问题**：`研究汇报_2026_08.html` 封面无版本号、日期与配套讲稿声明。第一轮已为两份盲区 deck 补此三项，
  而主线 deck 反而未补——**自订规则未施于最主要的一份交付物**。
- **依据**：答辩现场同时存在三份研究汇报稿（主线、双情境、开题），封面不标版本无法指认「今天讲的是哪一版」。
- **处置**：封面新增版本／日期／配套讲稿行。
- **复核**：`shoot_slides.py` 重截零溢出；`audit_all.py [B]` 页面结构通过。

### R3-02　投影上存在无文献来源的取值而未自认　（本轮最重）

- **问题**：主线汇报稿共出现十余处**研究者设定值**——分层级间间隔 1.5 s 档、撤销渐隐时长、
  制动威胁数阈值端点、事件间隔 60 s、行人肩宽 0.5 m、部分遮挡过渡段 0.15 s、
  最小侵入余量非劣界 −0.2 s、样本量 24 人、可靠性低水平等。
  这些值在 deck 上与文献锚点值**混排且不加区分**，读者无从判断哪一个有出处。
- **依据**：这是答辩中风险最高的一类追问：一旦某个值被问出「哪来的」而答不上，
  同一页上其余有出处的值也会连带失信。第一轮 R1-06 只解决了「投影数值是否被口播」，
  未解决「投影数值是否有来源」，属**同一风险的另一侧未覆盖**。
- **处置**：① 新增备查页 **p25「研究者设定值与不确定项清单」**，按时间参数／触发准则／场景与试次／
  刺激物理量／统计判定／盲区专属六类逐项列出「设定值—依据状态—处置」，并声明三条纪律
  （一律写入预注册；一律列入局限；凡可由预实验替换者以实测值替换）；
  ② 该页另主动交代一处不确定性——遮挡可预期性导致的效应量偏误**方向不明**，
  **不得单向断言本研究的估计偏保守**；③ 讲稿追加 p25 段并标「不讲，备查」。
  deck 由 24 页增至 **25 页**，讲稿头部口径同步改为「14 讲述 + 11 备查（p15–p25）」。
- **复核**：`shoot_slides.py` 25 页零溢出；`verify_deck_talk.py` 25 页标题逐字一致、配额合计 16 分 10 秒不变。

### R3-03　论文正文第 1 章仍是 v1 口径　（口径落后于上游）

- **问题**：`thesis/第1章_研究背景及意义.md` 通篇为 40 篇口径：研究问题写作
  「持续时长（RQ1）／级间间隔（RQ2）／新手对熟练（RQ3）」，空白数为八项，实验数为三个，
  核心目的写作「量化最优持续时长与最优级间时间间隔」。而第 2 章 v2 早已改为
  102 篇、九项空白、RQ1 参数化基准／RQ2 时间参数／RQ3 空间参数、五个实验、O1–O4。
- **依据**：第 1 章是论文的开篇，且第 2 章 §2.1 明确声明「与第 1 章平滑过渡」。
  两章口径不一致时，**读者以先读到者为准**，故这不是遗留问题而是当前的实质错误。
- **处置**：整章重写为 v2（约 5,100 字）：
  §1.1.3 以「绝对碰撞时间不可跨车速迁移」立论并引出零点替换；
  §1.2.1 给出九项空白清单并按**零点缺失／对照缺位／报告规范缺失**三种性质归类，
  分别对应「先做标定实验／做析因对照／声明口径而非再做实验」三种处置；
  §1.2.2 陈述三重约束模型并声明阶段框架**只用于组织可测阶段与因变量、不用于推导阶段耗时**；
  §1.2.3 给出 RQ1–RQ3 与 O1–O4 及其依赖结构；§1.3 三类意义各按四点／四点／三点展开；
  §1.4 章节结构改为十章；§1.5 新增记法与术语约定表（含四项显式声明）。
  §1.1.1 另确立一条与直觉相反的评价标准——预警的价值在于**制动位置前移与减速度下降**，
  而非按下踏板更快，并以提前 36.29 m／39.10 m 与峰值减速度上升 34.46% 两例正反对照。
- **复核**：`verify_style.py` 与 `audit_all.py [A]` 均通过；四类构念与 RQ／O 编号成套。

### R3-04　论文正文缺第 3 章，且第 1 章存在悬挂引用

- **问题**：`thesis/` 下无第 3 章。而重写后的第 1 章 §1.2.2 引用「§3.1 的设定值清单」、
  §1.4 列出「第 3 章 研究内容与预期目标」——形成**悬挂引用**。
  平行的盲区框架已有第 3 章，主线反而缺失。
- **依据**：第 3 章承担由综述到实验设计的过渡，是判定顺序、预注册条款与设定值清单的**唯一正式载体**；
  缺此章则 deck 上的 p25 清单在论文中无对应落点。
- **处置**：新建 `thesis/第3章_研究内容与预期目标.md`（约 3,600 字），六节：
  3.1 总目标与四项分目标（含两处限定语的实证依据）；3.2 五个实验分工、递进理由与**两条硬约束**
  （实验 1 与实验 2 须共用场景族并复用被试；实验 4 区块 F 的「固定时间参数」须限定为名义值并与实测校正值并列报告）；
  3.3 四条理论导出预测与十一条归纳假设的分工，含 H14 的跨实验整合限制与 H15 零结果的解释力；
  3.4 四类构念操作化与六级判定顺序；3.5 研究者设定值与不确定项清单（与 deck p25 同源）；
  3.6 预期成果四项与结论边界六条。
- **复核**：`verify_style.py` 通过；该文件已加入 `audit_all.py` 的 `DOCS` 扫描集，[A] 无残留。

### R3-05　PPTX 可能过期而无任何拦截机制

- **问题**：第二轮结束时 `研究汇报_2026_08.pptx` 由 24 页 deck 导出；R3-02 使 deck 增至 25 页后，
  该 pptx **静默过期**。`verify_pptx.py` 原有五项检查全部通过——因为它比对的是「旧 pptx 与旧 pptx 自身」，
  与源 deck 的新旧无关。这与 R1-04 的「报告过期」是同一类缺陷在另一环节的重现。
- **依据**：过期 PPTX 是会直接上台的产物，其危害高于任何一处文字瑕疵。
- **处置**：① `verify_pptx.py` 新增 **[0] 时效性**检查——`pptx.mtime < html.mtime - 1` 即判失败并提示重跑
  `html2pptx.py`；② 重导主线 pptx（25 页 / **424** 个可编辑对象）。
  同时把「凡改 deck 必重跑 `html2pptx.py`」与「凡改 deck 必重跑 `shoot_slides.py`」并列写入 README 纪律条款。
- **复核**：`verify_pptx.py --all` 五份全通过，[0] 至 [6] 共 35 项无一失败。

### R3-06　`verify_style.py` 把合规的反面引用判为违规

- **问题**：第 1 章 §1.5 的术语约定句「制动威胁数……**不得读作**基准时间节点」被判为旧术语残留。
  `audit_all.py` 的豁免词表在第一轮已收入「不得读作」，而 `verify_style.py` 的豁免表只有「不可读作」——
  **两个工具的豁免口径不一致**。
- **依据**：与 R1-07 同理，工具误报会稀释后续信号；且两工具口径不一致会导致同一句话在一处通过、在另一处失败。
- **处置**：`verify_style.py` 的 `EXEMPT` 补入「不得读作」，与 `audit_all.py` 对齐。
- **复核**：第 1 章两工具均通过；默认目标集四件无回归。

### R3-07　交付清单未纳入本轮全部产物

- **问题**：`README.md` 的文件结构与使用指南均停留在文献研究阶段：五份 HTML 汇报稿、
  五份讲稿、五份 PPTX、`thesis/` 三章、`盲区框架/` 子目录，以及
  `html2pptx.py`／`verify_pptx.py`／`audit_all.py`／`audit_round2.py`／`verify_deck_talk.py`／`verify_style.py`
  六个脚本**一处未提**。
- **依据**：交付清单的作用是使第三方在不询问作者的情况下找到产物并复现校验；
  清单缺项等于这些产物在项目外部不存在。
- **处置**：README 新增 **「汇报交付物与校验入口」** 一节——五份三件套的页数与配额对照表、
  两轨制计时口径与数值口播约定、八条校验命令、三条纪律（改 deck 必重截图、必重导 pptx、
  盲区综述须改生成脚本）与一条已知限制（无 LibreOffice，PPT 视觉保真不可渲染核验）；
  文件结构树补入呈现与论文正文两组条目及六个脚本；使用指南新增六行（上台放映、
  在 PowerPoint 里改版式、查审核处置、写论文第 1／3 章、平行框架入口）。
- **复核**：README 章节标题连续，`关键新证据速查` 等原有小节完整。

### R3-08　全链路终检

- **范围**：五份 deck 重截、五对 deck–讲稿一致性、表达禁则、两轮审计、盲区七项、五份 PPTX 七项。
- **结果**：
  - `shoot_slides.py`：25 / 37 / 13 / 14 / 14 页，**溢出 0 页**；
  - `verify_deck_talk.py`：五对全通过，配额 16:10 / 33:10 / 19:40 / 20:50 / 17:00；
  - `verify_style.py`：默认四件全通过；
  - `audit_all.py`：[A] 无残留、[B] 标题唯一页码连续、[D] 无错值、[E] 六组编号齐全；
  - `audit_round2.py`：[F] 引注全可追溯、[G] 全页压缩版可行、[H] 备答覆盖 **100%**、[I] 余两页为已声明例外；
  - `verify_pptx.py --all`：五份全通过；
  - `盲区框架/scripts/verify_blind.py`：七项全通过。
- **遗留（非本轮可闭合）**：① 12 条盲区补检文献仍停留题录级（抓取被 403 拒绝），
  已在证据台账标注「仅题录，待补全」且不得作为数值依据；② 不透明度记法冲突（G8）须取得全文方可裁定；
  ③ 无 LibreOffice，PPT 视觉保真只能人工抽查。

---

## 附录 A　审核对象清单

| # | 交付物 | 规模 | 配套 |
|---|---|---|---|
| 1 | `研究汇报_2026_08.html` | **25 页**（14 讲述 + 11 备查 p15–p25） | 讲稿 25 页 / 配额 16 分 10 秒；`研究汇报_2026_08.pptx`（25 页 / 424 对象） |
| 2 | `文献综述_幻灯片.html` | 37 页 | 讲稿 37 页 / 配额 33 分 10 秒；`文献综述_幻灯片.pptx`（37 页 / 674 对象） |
| 3 | `盲区框架/研究汇报_双情境框架.html` | 13 页 | 讲稿 13 页 / 配额 19 分 40 秒；同名 `.pptx` |
| 4 | `盲区框架/开题报告_双情境框架.html` | 14 页 | 讲稿 14 页 / 配额 20 分 50 秒；同名 `.pptx` |
| 5 | `盲区框架/文献综述_盲区情境.html` | 14 页 | 讲稿 14 页 / 配额 17 分 0 秒；同名 `.pptx` |
| 6 | 主线论文正文 | `thesis/` 第 1 章（v2，本轮重写）、第 2 章 v2、第 3 章（本轮新建） | 五份实验设计依据（实验 1–5） |
| 7 | 盲区框架论文正文 | `盲区框架/thesis/` 第 1 章、专题综述（2B.1–2B.7）、第 3 章 | 三份实验设计依据（实验 1／4／5） |

## 附录 B　校验入口

```bash
python3 scripts/shoot_slides.py <deck.html> <out>   # 截图 + 溢出探针（out 不带 figures/ 前缀）
python3 scripts/verify_deck_talk.py <deck> <talk>   # 页数、标题逐字、分块、计时
python3 scripts/verify_style.py                     # 表达禁则、构念齐备、编号成套
python3 scripts/audit_all.py                        # 旧术语、页面结构、数值一致、编号
python3 scripts/audit_round2.py                     # 引注可追溯、配额可行性、备答覆盖、单页字数
python3 scripts/html2pptx.py --all                  # 五份 deck → 可编辑 PPTX
python3 scripts/verify_pptx.py --all                # 时效性、页数、文本覆盖、越界、溢出、行高、可编辑性
python3 盲区框架/scripts/verify_blind.py             # 盲区目录七项
```

## 附录 C　三轮审核的问题类型分布

| 轮次 | 主题 | 条数 | 其中属**工具缺陷**者 | 其中属**口径落后**者 |
|---|---|---|---|---|
| 第一轮 | 结构与口径一致性 | 8 | 2（R1-07、R1-08） | 1（R1-04 报告过期） |
| 第二轮 | 计时可行性、引注可追溯与应答覆盖 | 5 | 1（R2-01 的校验器改写） | 0 |
| 第三轮 | 可追溯性、口径同步与交付完整性 | 8 | 2（R3-05、R3-06） | 2（R3-03、R3-05 的 pptx 过期） |

三轮下来有一条规律值得记入方法：**二十一条意见中有五条是校验工具自身的缺陷、三条是产物落后于上游。**
即在已有机检的条件下，缺陷的主要形态不再是「写错」，而是「**检查的东西不对**」与「**检查的时点不对**」。
故后续每次改动交付物，须同时问两句：这一改动是否使某个既有报告过期；现有检查项是否覆盖这一改动引入的新风险。

