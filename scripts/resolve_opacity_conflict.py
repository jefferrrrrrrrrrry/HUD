#!/usr/bin/env python3
"""裁定冲突六（不透明度记法），并把结论写回 §2.3.5 与 §2.4.7。

裁定依据（本轮新取得的一手证据）
  Ye 等（2025，*Electronics* 14(23):4768，开放获取，本库 #09）在方法节写：
    "The color transparency of the graphics was set to **0.75**, which offers
     advantages in **visibility** and search response [22,23]."
  其中 [23] 正是 Hou 等（2024，本库 #48），[22] 为 Hussain 与 Park（2023）。
  该句同时把 #48 的"闪烁"结论用作自己的动效取值依据（"the dynamic effect …
  was set to blinking [23]"），可见 #09 确实读过 #48 全文。

  **决定性推论**：一个"提升 visibility（可见性）"的 0.75，在物理上只能是
  alpha／不透明度。若 0.75 意为"75% 透明"（即仅 25% 不透明），图形近乎不可见，
  不可能被描述为提升可见性。故 #48 的 T 标尺虽字面写 transparency，
  **功能上是 alpha（不透明度）**：T1 = 完全不透明，T0.35 = 最透明。

  据此四篇的取值全部落在同一区间：
    #48 最优 0.5–0.75（T0.75 无意视盲最低、T0.5 反应时最短）
    #52 最优 ≥ 0.6（10 档梯度 0.1–1.0，0.7 以上不受背景复杂度影响）
    #09 实取 0.75（引 #48 + Hussain 与 Park 2023）
    #53 的 **60%** 与上述一致；其 20% 是**另一构念**上的发现

  #53 的正确读法（原记录有误）：摘要原文为 "opacity levels of 20 % and 60 %
  **both** support situation awareness"——它并非推荐 20%，而是报告两值**都**
  可支持情境意识，且自身结论是 "drivers are ambivalent" 与 "need to be further
  investigated"。故 #53 与 #52 在 0.6 上**并不冲突**；差异在测量构念：
  #52 测阅读 AR 的反应时（越不透明越快），#53 测对路侧环境的情境意识
  （越不透明遮挡越多）。这是同一参数上的**权衡**，不是矛盾。

  因此冲突六从"三方 3 倍冲突、不可裁定"降级为"记法已裁定、四源收敛于
  0.6–0.75，唯一低值出自不同构念且该文自身不作推荐"。
"""
from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
P = ROOT / "thesis" / "_parts"

# ── §2.3.5：表 2-7 与三处问题整体改写 ────────────────────────────────────
OLD_2_3_5_TABLE = """| 来源 | 记法 | 报告值 | 测量对象 | 证据强度 |
|---|---|---|---|---|
| Ye 与 Yin（2025） | 小数 | 0.75 | 空间面定位与无意视盲 | 全文 |
| **Hou 等（2024）** | T 值 | **T1 / T0.75 / T0.5 / T0.35** 四档 | 动效 × 色彩透明度对无意视盲 | 仅摘要 |
| **Li 等（2025a）** | 小数 | **0.1–1.0** 梯度；**≥ 0.6** 与 **0.7** 两个分界 | 驾驶背景复杂度 × 界面不透明度 | 仅摘要 |
| **Lopez 与 Moacdieh（2025）** | 百分比 | **20%** 与 **60%** | 不透明度偏好、视觉注意与情境意识（N = 27） | 仅摘要 |"""

NEW_2_3_5_TABLE = """| 来源 | 原文记法 | 报告值 | 换算为不透明度 | 测量构念 | 证据强度 |
|---|---|---|---|---|---|
| Ye 与 Yin（2025） | transparency | 0.75（实取值） | **0.75** | 空间面定位与无意视盲 | 全文 |
| **Hou 等（2024）** | color transparency | **T1 / T0.75 / T0.5 / T0.35** | **1 / 0.75 / 0.5 / 0.35**（见下方裁定） | 无意视盲、反应时、满意度 | 仅摘要 |
| **Li 等（2025a）** | opacity | **0.1–1.0** 十档；**≥ 0.6**、**0.7** 两分界 | **≥ 0.6** | 反应时、瞳孔直径、AOI 注视时长 | 仅摘要 |
| **Lopez 与 Moacdieh（2025）** | opacity | **20%** 与 **60%** | **0.2 与 0.6** | 偏好 + **情境意识**（视频观看，N = 27） | 仅摘要 |"""

OLD_2_3_5_BODY = """**三处必须显式记录的问题**：

（1）**"透明度"与"不透明度"在摘要层面无法区分**。Hou 等（2024）用 T 值（T1 可能指完全不透明也可能指完全透明），Li 等（2025a）明确写 opacity（不透明度），Lopez 与 Moacdieh（2025）也写 opacity 但给出 20% 这一低值。若后者的 20% 指不透明度，则与前者的 ≥ 0.6 **相差 3 倍**；若指透明度（即不透明度 80%），则两者方向一致而数值仍不同。**本研究在正文引用这一组数值前，必须至少取得一篇全文以确定记法方向**——这是本课题空间侧优先级最高的待补事项。

（2）**主观偏好与客观绩效可能本就不一致**。Lopez 与 Moacdieh（2025）测的是**偏好**，Li 等（2025a）测的是**客观视觉认知绩效**。人因研究中偏好与绩效的解离是常见现象，因此两者数值不同并不必然构成矛盾，但**不能把二者混合引用为"推荐不透明度"**。

（3）**既有转述已出现失真**，见 §2.2.4 对 Hou 等（2024）的更正。"""

NEW_2_3_5_BODY = """#### （1）记法方向的裁定

本研究由一条**间接但决定性**的证据裁定了 Hou 等（2024）的 T 标尺方向。Ye 与 Yin（2025）在方法节写道："图形的色彩透明度设为 **0.75**，该值在**可见性**与搜索响应上具有优势［22, 23］"，其中文献［23］正是 Hou 等（2024），［22］为 Hussain 与 Park（2023）；同一段还把 Hou 等的闪烁结论用作自身动效取值的依据。**一个"提升可见性"的 0.75 在物理上只能是 alpha（不透明度）**——若它意为"75% 透明"（即仅 25% 不透明），图形近乎不可见，不可能被描述为提升可见性。

因此：**Hou 等（2024）的 T 标尺虽字面写作 transparency，功能上是 alpha**，T1 为完全不透明、T0.35 为最透明。其最优区间（T0.75 无意视盲最低、T0.5 反应时最短且满意度最高）换算为不透明度 **0.5–0.75**。

#### （2）三倍冲突的消解：构念差异而非数值矛盾

裁定记法后，四篇的取值全部落入同一区间：Hou 等（2024）0.5–0.75、Li 等（2025a）≥ 0.6、Ye 与 Yin（2025）实取 0.75、Lopez 与 Moacdieh（2025）的 **60%**。

关于 Lopez 与 Moacdieh（2025）的 20%，本研究此前的记录有一处**读法错误须更正**：该文摘要原文为"20% 与 60% 两个不透明度水平**均**支持情境意识"，它**并非推荐 20%**，而是报告两值都够用；该文自身的结论是"驾驶员对不透明度态度矛盾（ambivalent）"与"仍需进一步研究"，**未给出推荐值**。因此它与 Li 等（2025a）在 0.6 上**并不冲突**。

两者的真正差异在**测量构念**，而这一差异恰好揭示了不透明度的作用机制：

| | Li 等（2025a） | Lopez 与 Moacdieh（2025） |
|---|---|---|
| 测什么 | 阅读 AR 图形的**反应时**、瞳孔、AOI 注视时长 | 对**路侧环境**的情境意识 + 主观偏好 |
| 不透明度升高的效果 | **有利**（图形更易读，0.7 以上不再受背景复杂度影响） | **不利**（对真实场景的遮挡增加） |
| 由此得出 | 下界 ≥ 0.6 | 0.2 已足以保住路侧情境意识 |

**不透明度是一个在"AR 图形可读性"与"真实场景遮挡度"之间权衡的参数**。0.6 之所以是可辩护的取值，正因为它同时满足了两侧：达到 Li 等给出的可读性下界，又落在 Lopez 与 Moacdieh 验证过情境意识可保持的水平上。

#### （3）尚存的不确定性与本研究的处理

裁定仍属**间接推断**（依据是引用者的转述，而非 Hou 等原文的参数定义），故本研究：① 在正文标注该裁定的证据性质；② 实验 3 主条件取 **0.6**（四源交集的保守下界，而非 0.75），预实验保留 **0.2** 档作敏感性检验——但检验目的已从"消解记法不确定性"改为"**检验低不透明度的遮挡收益是否超过可读性代价**"，这是一个有明确机制预期的检验；③ 完整报告本研究自身的记法方向与 Michelson 对比度。

另需记录一处**文献覆盖缺口**：Hussain 与 Park（2023，*IJHCI*）专门研究"透明度水平 × 真实世界背景"，与本参数直接相关，但**不在本库 102 篇内**（检索式未覆盖"transparency level"作为主题词）。本研究在局限中声明这一漏检。

#### （4）既有转述已出现失真

见 §2.2.4 对 Hou 等（2024）的更正。"""

# ── §2.4.7：冲突六整体改写 ──────────────────────────────────────────────
OLD_2_4_7 = """这是一组典型的 B 类冲突——数值相差 3 倍，但可能只是记法方向不同。详细证据表见 §2.3.5 表 2-7，此处只给辨析结论。

**四种可能的解释路径**：

| 路径 | 假设 | 若成立则 |
|---|---|---|
| 1 | Lopez 与 Moacdieh（2025）的 20% 指**透明度**（即不透明度 80%） | 与 Li 等（2025a）的 ≥ 0.6 方向一致，数值仍不同但不构成矛盾 |
| 2 | 20% 指**不透明度** | 与 ≥ 0.6 相差 3 倍，构成实质冲突 |
| 3 | 两者测量对象不同（偏好 vs 客观绩效） | 数值不可比，须分别报告 |
| 4 | Hou 等（2024）的 T 值方向未知 | 该篇四档数值暂不可用 |

**本研究的处理（不回避、不平均）**：实验 3 的主条件取 **0.6**（Li 等 2025a 的分界值），并在预实验中**同时测 0.2**（Lopez 与 Moacdieh 2025 的报告值）；**若两值下的条件排序不一致，则在正文中作为局限报告，而不是选择支持自己结论的那一个**。同时把"取得 Li 等（2025a）或 Lopez 与 Moacdieh（2025）全文以确定记法方向"列为本课题空间侧优先级最高的待补事项。

**这一处理方式本身具有方法学意义**：当文献记法不统一而又无法立即取得全文时，正确做法是把不确定性显式转化为**敏感性检验**，而不是在正文中挑一个数值当作定论。"""

NEW_2_4_7 = """这是一组典型的 B 类冲突——表面上数值相差 3 倍。**本研究已将其裁定**，过程见 §2.3.5 第（1）（2）小节，此处只给结论与方法学含义。

**裁定结果分两步**：

**第一步，记法方向已定。** Ye 与 Yin（2025）在引用 Hou 等（2024）时把其 T 标尺当作"提升**可见性**的 0.75"使用——一个提升可见性的 0.75 只能是不透明度而非透明度。故 Hou 等（2024）的最优区间换算为不透明度 **0.5–0.75**。

**第二步，剩余差异不是矛盾而是构念差异。** Lopez 与 Moacdieh（2025）的原文是"20% 与 60% **均**支持情境意识"，**并未推荐 20%**，且明确表示"仍需进一步研究"。它与 Li 等（2025a）在 **0.6** 上一致；两者的分歧只出现在低值端，而那源于测量的是不同构念——Li 等测**阅读 AR 图形的速度**（不透明度越高越快），Lopez 与 Moacdieh 测**对路侧环境的情境意识**（不透明度越高遮挡越多）。

**表 2-7a　冲突六裁定前后的证据状态**

| | 裁定前的记录 | 裁定后 |
|---|---|---|
| 记法方向 | 未知，四篇均未明确 | **已定**：T 标尺功能上为 alpha（间接证据） |
| 数值状态 | 三方 3 倍冲突，不可裁定 | **四源收敛于 0.6–0.75** |
| 20% 的地位 | 与 ≥ 0.6 冲突的推荐值 | **不是推荐值**；是另一构念上的充分性发现 |
| 对设计的影响 | 取值缺乏依据 | **0.6 有两条独立支持**（可读性下界 + 情境意识可保持） |

**本研究的处理（更新后）**：实验 3 主条件取 **0.6**——它是四源交集的保守下界，同时满足可读性与遮挡两侧约束。预实验保留 **0.2** 档，但检验目的已改变：从"消解记法不确定性"变为"**检验低不透明度的遮挡收益是否超过可读性代价**"，这是一个有明确机制预期、可否证的检验。

**这一案例的方法学含义有两层**，比原先的表述更强：

第一层仍成立——当记法不统一而无法立即取得全文时，应把不确定性显式转化为敏感性检验，而不是挑一个数值当定论。

第二层是本轮新得到的：**B 类冲突有时可以由"引用者如何使用该参数"间接裁定**。当原文不可得时，一篇**引用了它并复现其设置**的开放获取文献，往往在方法节里无意间保留了记法方向的判据。这条路径的代价是证据等级降为间接，但它把一个"完全不可用"的参数组恢复为"可用但须标注"。

**同时须记录一处检索漏检**：Hussain 与 Park（2023，*IJHCI*）专题研究"透明度水平 × 真实世界背景"，与本参数直接相关却不在本库 102 篇内，原因是检索式未把 transparency level 作为主题词。这是本研究文献覆盖的一处真实局限，已在 §2.6 声明。"""

FIXES = [
    ("sec2_3.md", OLD_2_3_5_TABLE, NEW_2_3_5_TABLE),
    ("sec2_3.md", OLD_2_3_5_BODY, NEW_2_3_5_BODY),
    ("sec2_4.md", OLD_2_4_7, NEW_2_4_7),
    # 冲突汇总表一行
    ("sec2_4.md",
     "| 六：不透明度 3 倍冲突 | B | **记法方向**；偏好 vs 绩效 | 主条件 0.6 + 预实验测 0.2 的敏感性检验 |",
     "| 六：不透明度 3 倍冲突（**本轮已裁定**） | B | **记法方向**（已定为 alpha）；**可读性 vs 遮挡**两构念 | 四源收敛 0.6–0.75；主条件 0.6 + 0.2 档改为检验遮挡收益 |"),
    # §2.4.10 方法学评述中的表述
    ("sec2_4.md",
     "| **不透明度的记法方向** | 四篇均未明确 | 产生 §2.4.7 的 3 倍数值冲突 |",
     "| **不透明度的记法方向** | 四篇均未明确 | 曾产生 3 倍数值冲突；本研究经引用链间接裁定（§2.4.7），但**须由原文报告方能根本解决** |"),
    # §2.5.1 研究空白 G8 降级
    ("sec2_3.md",
     "2. **不透明度记法冲突未裁定**，直接影响实验 3 四个 AR 条件的视觉参数取值。",
     "2. **不透明度记法冲突已由引用链间接裁定**（§2.3.5 第 1 小节），四源收敛于 0.6–0.75；但裁定依据为引用者转述而非原文参数定义，仍须取得 Hou 等（2024）或 Li 等（2025a）全文方能根本确认。"),
    # §2.2.4 的措辞与新裁定衔接
    ("sec2_3.md",
     "**（3）透明度—风险联动**。Ye 与 Yin（2025）把透明度设为 0.75；",
     "**（3）透明度—风险联动**。Ye 与 Yin（2025）把不透明度设为 0.75（原文写 transparency，见 §2.3.5 的记法裁定）；"),
]


def main() -> None:
    n = 0
    for fname, old, new in FIXES:
        p = P / fname
        s = p.read_text(encoding="utf-8")
        if new.split("\n")[0][:40] in s:
            print(f"SKIP {fname}: {old[:36]}…（已改）")
            continue
        if old not in s:
            print(f"⚠ MISS {fname}: 未找到 {old[:46]}…")
            continue
        p.write_text(s.replace(old, new, 1), encoding="utf-8")
        n += 1
        print(f"OK   {fname}: {old[:44]}…")
    print(f"\n共改写 {n} 处；请重跑 scripts/build_chapter2.py 重建正文")


if __name__ == "__main__":
    main()
